# PRB Demo Sites

Static demo websites used for PRB testing (desktop + browser extension).

Goals:
- realistic auth flows
- distinct HTTPS origins
- static hosting (GitHub Pages / Netlify)
- shared minimal UI

Each site folder is deployable independently.

